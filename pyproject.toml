[project]
name = "bw-kp-sync"
version = "0.1.0"
description = "BitWarden-KeePass sync"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "black>=25.1.0",
    "mypy>=1.15.0",
    "pytest>=8.3.5",
]
lint = [
    "ruff"
]
